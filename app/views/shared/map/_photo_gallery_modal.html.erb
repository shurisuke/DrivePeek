<%# 写真ギャラリーモーダル（Stimulusで制御） %>
<div class="photo-gallery-modal"
     hidden
     data-controller="infowindow--photo-gallery"
     data-action="photo-gallery:open@document->infowindow--photo-gallery#open keydown.esc->infowindow--photo-gallery#close keydown.left->infowindow--photo-gallery#prev keydown.right->infowindow--photo-gallery#next">
  <div class="photo-gallery-modal__overlay" data-action="click->infowindow--photo-gallery#close"></div>
  <div class="photo-gallery-modal__content">
    <%# タイトル（上部中央） %>
    <div class="photo-gallery-modal__header">
      <span class="photo-gallery-modal__title" data-infowindow--photo-gallery-target="title"></span>
    </div>

    <%# 左矢印ボタン %>
    <button type="button"
            class="photo-gallery-modal__arrow photo-gallery-modal__arrow--prev"
            data-infowindow--photo-gallery-target="prevBtn"
            data-action="click->infowindow--photo-gallery#prev"
            hidden>
      <i class="bi bi-chevron-left"></i>
    </button>

    <div class="photo-gallery-modal__slider"
         data-infowindow--photo-gallery-target="slider"
         data-action="scroll->infowindow--photo-gallery#onScroll">
      <%# スライドはJSで動的に生成 %>
    </div>

    <%# 右矢印ボタン %>
    <button type="button"
            class="photo-gallery-modal__arrow photo-gallery-modal__arrow--next"
            data-infowindow--photo-gallery-target="nextBtn"
            data-action="click->infowindow--photo-gallery#next"
            hidden>
      <i class="bi bi-chevron-right"></i>
    </button>

    <%# フッター（インジケーター + 閉じるボタン） %>
    <div class="photo-gallery-modal__footer">
      <div class="photo-gallery-modal__indicator" data-infowindow--photo-gallery-target="indicator">
        <%# ドットはJSで動的に生成 %>
      </div>
      <button type="button" class="photo-gallery-modal__close-btn" data-action="click->infowindow--photo-gallery#close">
        閉じる
      </button>
    </div>
  </div>
</div>
