<%= @resource.email %> 様

DrivePeekをご利用いただきありがとうございます。

パスワード再設定のリクエストを受け付けました。以下のリンクからパスワードを変更できます。

<%= edit_password_url(@resource, reset_password_token: @token) %>

このリクエストに心当たりがない場合は、このメールを無視してください。
上記のリンクにアクセスしない限り、パスワードは変更されません。

---
DrivePeek
