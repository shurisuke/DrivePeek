<%# 提案モード選択UI %>
<div class="suggestion-mode-select" data-controller="suggestion--mode">
  <button type="button"
          class="suggestion-mode-select__btn"
          data-action="click->suggestion--mode#startPlanMode">
    <i class="bi bi-share"></i>
    まるっとプラン提案
  </button>
  <button type="button"
          class="suggestion-mode-select__btn"
          data-action="click->suggestion--mode#startSpotMode">
    <i class="bi bi-search"></i>
    かこんでスポット検索
  </button>
</div>
