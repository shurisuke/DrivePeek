<%# 提案モード選択UI %>
<div class="suggestion-mode-select" data-controller="suggestion-tab--mode">
  <button type="button"
          class="suggestion-mode-select__btn"
          data-action="click->suggestion-tab--mode#startPlanMode">
    <i class="bi bi-share"></i>
    まるっとプラン提案
  </button>
  <button type="button"
          class="suggestion-mode-select__btn"
          data-action="click->suggestion-tab--mode#startSpotMode">
    <i class="bi bi-search"></i>
    かこんでスポット検索
  </button>
</div>
