<%# 提案モード選択UI %>
<div class="ai-mode-select" data-controller="suggest-mode">
  <button type="button"
          class="ai-mode-select__btn"
          data-action="click->suggest-mode#startPlanMode">
    <i class="bi bi-map"></i>
    まるっとプラン提案
  </button>
  <button type="button"
          class="ai-mode-select__btn"
          data-action="click->suggest-mode#startSpotMode">
    <i class="bi bi-geo-alt"></i>
    エリアからスポットを提案
  </button>
</div>
