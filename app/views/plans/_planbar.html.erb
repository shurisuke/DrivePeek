<aside class="planbar" data-controller="navibar">
  <div class="plan-tab-nav">
    <button class="tab-btn tab-community"
            data-tab="community"
            data-navibar-target="button"
            data-action="click->navibar#switch">
      みんなの旅
    </button>

    <button class="tab-btn active"
            data-tab="plan"
            data-navibar-target="button"
            data-action="click->navibar#switch">
      プラン
    </button>

    <button class="tab-btn tab-ai"
            data-tab="ai"
            data-navibar-target="button"
            data-action="click->navibar#switch">
      AI提案
    </button>
  </div>

  <div class="planbar__content" id="planbar_content">
    <%# ✅ スクロールコンテナ: OFF時350px / ON時430px %>
    <div class="planbar__content-scroll">
      <%# ブロック一覧（常時350px） %>
      <div class="planbar__blocks" id="planbar_blocks">
        <%= render "plans/planbar_content", plan: plan %>
      </div>
    </div>
  </div>

  <div id="planbar_footer">
    <%= render "plans/planbar_footer", plan: plan %>
  </div>
</aside>
